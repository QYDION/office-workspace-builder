# 更新日志

## 1.0.1

- **修复**：机器上没装 Node.js 时，尺子自检会把「JS 语法错误」样本判成漏检，进而报出「检查器已损坏」，让 `verify.py` 整体失败、退出码 1 —— 这和 README 里「Node 可选，没装会自动跳过并提示」的说法自相矛盾，也让只装了 Python 的人第一次跑就被误导。现在该用例在无 Node 时记为**跳过**（不计入失败、退出码保持 0），并明确说明哪一部分没有被验证。
- **改进**：是否给输出上色改由终端类型决定 —— 重定向到日志文件或管道时不再写入 ANSI 转义序列，同时支持 `NO_COLOR` 环境变量。
- **改进**：控制台分隔线改用 ASCII 字符，避免在非 UTF-8 代码页的终端下显示成乱码。
- **文档**：明确要求脚本以技能根目录为工作目录运行，并把生成文件的**绝对路径**传给脚本，避免相对路径在别的客户端里解析失败。

## 1.0.0

- 首个公开版本。
- 13 条铁律：全内联零外链、规模闸门、单向调用架构、失败必须可见、设计不同质化、文件操作纪律等。
- 7 份参考资料：需求解析与生成顺序、20+ 办公模块规格、配色与版式纪律、宿主能力适配、迭代红线与 CSV 导入、完整范例走查。
- 1367 行骨架模板，自带一个完整示例模块与两个占位槽，已通过全部校验。
- 三道交付前验证闸门：尺子自检（检查器先反测）、静态检查、运行时检查（DOM 桩真跑内联 JS）。
- 默认走宿主在线存储能力部署并交付在线链接，能力不可用时降级为本地 HTML + 本地存储。
